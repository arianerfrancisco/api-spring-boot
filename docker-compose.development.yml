version: "3.9"
services:
  db:
    image: postgres
    volumes:
      - db_data:/var/lib/postgres
    restart: always
    env_file:
      - config/db-dev.env

  api-cake-dev:
    depends_on:
      - db
    build: .
    ports:
      - "8082:8082"
    restart: always

volumes:
  db_data: {}